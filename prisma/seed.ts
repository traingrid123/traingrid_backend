async function main(): Promise<void> {
  console.log("Seed placeholder. Add your data after the schema is finalized.");
}

main().catch((error) => {
  console.error("Seed failed:", error);
  process.exit(1);
});
